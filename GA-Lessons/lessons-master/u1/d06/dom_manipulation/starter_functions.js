// ------------------------------------------------------------
// Parameters: coll is something you can iterate over in a for loop.

// Returns the contents of coll in a new array.
var intoArray = function (coll) {

};

// ------------------------------------------------------------
// Parameters: element is a DOM element; text is a string.

// Sets the innerText of element to text.

// Returns element.
var setInnerText = function (element, text) {

};

// ------------------------------------------------------------
// Parameters: parentElement is a DOM elmement. tag is a string tag name (ie, "div", "p", etc).

// This function should create a new DOM element with its tag name set
// to tag, and append it as a child to parentElement.  

// Returns the new DOM element.
var appendNewElement = function (parentElement, tag) {

};

// ------------------------------------------------------------
// Parameters: parentElement is a DOM element.

// Returns the child elements of parentElement *as an array* (not a Node list).
var childrenArray = function (parentElement) {

};

// ------------------------------------------------------------
// Parameters: element is a DOM element. color is a string specifying
// a color ("red", "blue", "rgb(200, 100, 0)", etc).

// This function sets the background color of element to color.

// Returns element.
var setBackgroundColor = function (element, color) {

};
